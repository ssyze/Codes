#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e6 + 5;
int a[maxn], n, m, ans[maxn];
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    while (cin >> m >> n) {
        for (int i = 0; i < n; i++) cin >> a[i];
    }
}